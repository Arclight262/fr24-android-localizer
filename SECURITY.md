# 安全策略

## 支持范围

本项目仍处于测试阶段。安全修复只面向默认分支的最新代码和最新公开测试版本；较早构建可能不会单独维护。

## 报告安全问题

不要在公开 Issue 中披露可被利用的细节、密钥、完整设备日志、设备序列号、原版 APK 或反编译产物。

优先使用仓库 Security 页面中的私密漏洞报告功能。如果该入口不可用，可创建一个不含敏感细节的公开 Issue，请维护者提供私下沟通方式。

报告中请包含受影响版本、影响说明、最小复现步骤和已脱敏的必要证据。请勿利用问题访问不属于你的设备、账户或数据。

## 项目边界

本模块只负责 Flightradar24 Android 客户端的界面文字替换，不提供订阅、支付、完整性校验、反作弊或 Root 检测绕过。与这些绕过有关的报告不属于本项目支持范围。
